{
  "name": "person-1-frontend-gateway",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix api-gateway && npm install --prefix frontend",
    "dev": "concurrently -n gateway,frontend -c cyan,magenta \"npm run dev --prefix api-gateway\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
